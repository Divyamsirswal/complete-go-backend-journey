## Difference between & and *
* ```&``` -> it is use to take the address of the variable or any identifier you have defined 
* ```*``` -> when you initialize a variable with it it is used as a pointer to a variable and after when you use it it is use to access the variable value from the address 

## Why pointer is used in functions
* when you don't  want the copy, when you share some data to the function you have choice either pass it by value or pass it by actuall address -> here comes the pointer -> which is used in function

## What happens if we don’t use pointer
* Nothing will happen the things are same but the main thing is that if you want to work closer to the ram and usage and see what is the address and manupulate its value so pointers are very good in that and best usage can be seen in while passing variables in function

## Pointer vs value — when to use which
* According to the need of mine i will choose it for sure there is no hard and fast rule i have to choose one you have to be hybrid and choose accordingly which is best for you