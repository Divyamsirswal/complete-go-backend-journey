## Why does ```Scan()``` needs ```&``` ?
* Go needs address to store input inside variable

## Difference between Print, Println, Printf
* ```Print``` -> no new line only print at point 
* ```Println``` -> print and add a new line after it
* ```Printf``` -> use to print things in well formatted manner

## What happens if user enters "abc" instead of number?
* No matter what to do instead of a number go has a feature of zero value so that entry is converted to 0.

## Why is formatting important in real applications?
* it gives very clear understand what u getting from outside is that correct or the logic u have written given correct output so that become really easy for debugging also 