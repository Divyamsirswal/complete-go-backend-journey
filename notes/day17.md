## Why database is needed ?
* to store data secure and parmanent too

## Difference between memory vs database ?
* ``memory`` -> temporary
* ``database`` -> are parmanent and more better

## What is a table 
* its a row x col where each col is record and each col is fields

## What is primary key
* its a unique + not null field in the table 