## What is HTTP request/response cycle?
* everthing you click on browser or any link out there all having req/res cycle 
* like mobile -> req -> google -> server -> res -> google -> res -> mobile

## What is handler function?
* this function actually whatever route we have -> it has some permission -> you have headers -> you have methods -> body and all so you can maintain these kinda stuff in handler functions only.

## Difference between ResponseWriter and Request
* ``ResponseWriter`` -> is what we send back to client
* ``Request`` -> is what we get from the client

## Why backend servers listen on ports?
* i guess server need to have a stograge like for the traffics network calls and other things to work on